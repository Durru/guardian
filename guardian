#!/usr/bin/env python3
"""Thin wrapper — delegates to lib/guardian.py."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent / "lib"))
from guardian import main

sys.exit(main())
